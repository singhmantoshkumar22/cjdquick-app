/**
 * AI/ML Types for OMS Intelligence Layer
 *
 * Type definitions for:
 * - Anomaly Detection (fraud, errors, patterns)
 * - Demand Forecasting (SKU-level predictions)
 * - Control Tower (SLA prediction, capacity planning)
 *
 * Note: Implementation is in the FastAPI backend.
 * Frontend fetches data via /api/v1/* catch-all proxy.
 */

export * from './types';
