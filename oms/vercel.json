{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "framework": "nextjs",
  "installCommand": "npm install -g pnpm@8 && pnpm install --filter @oms/database --filter web --no-frozen-lockfile",
  "buildCommand": "cd packages/database && npx prisma generate && cd ../.. && pnpm --filter web build",
  "outputDirectory": "apps/web/.next"
}
