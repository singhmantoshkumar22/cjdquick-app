{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd apps/web && SKIP_ENV_VALIDATION=1 npx next build",
  "outputDirectory": "apps/web/.next",
  "framework": null,
  "env": {
    "SKIP_ENV_VALIDATION": "1"
  }
}
